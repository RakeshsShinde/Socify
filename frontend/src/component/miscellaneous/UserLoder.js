import { Skeleton } from '@mui/material';
import React from 'react';

const UserLoder = () => {
    return (
        <Skeleton width={'100%'} height={'80px'} animation={'wave'}>

        </Skeleton>
    );
}

export default UserLoder;
